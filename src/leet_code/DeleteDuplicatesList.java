package leet_code;
/*
@Author: Vishnu Kumar
@Date : 2025-06-13
@Problem: LeetCode - Delete Duplicates from Sorted List Type Easy
@Problem Explanation
You're given a sorted linked list, and your task is to remove all duplicate elements from it. The list is sorted in ascending order, so duplicates will be adjacent to each other.
You need to modify the list in place, meaning you should not use extra space for another list. The final list should contain only unique elements, and it should still be sorted.
Examples:
Input: 1 -> 1 -> 2 -> 3 -> 3
Output: 1 -> 2 -> 3
Input: 1 -> 1 -> 1 -> 2 -> 3
Output: 1 -> 2 -> 3
 */
public class DeleteDuplicatesList {
    public static void main(String[] args) {
//        // Example usage
//        ListNode head = new ListNode(1);
//        head.next = new ListNode(1);
//        head.next.next = new ListNode(2);
//        head.next.next.next = new ListNode(3);
//        head.next.next.next.next = new ListNode(3);
//
//        DeleteDuplicatesList solution = new DeleteDuplicatesList();
//        ListNode result = solution.deleteDuplicates(head);
//
//        // Print the result
//        while (result != null) {
//            System.out.print(result.val + " ");
//            result = result.next;
//        }
        // Example usage


        //

    }
//    public ListNode deleteDuplicates(ListNode head) {
//        if (head == null) return null; // If the list is empty, return null
//
//        ListNode current = head; // Start with the head of the list
//
//        while (current != null && current.next != null) {
//            if (current.val == current.next.val) {
//                // If the current node's value is equal to the next node's value,
//                // skip the next node by pointing current.next to current.next.next
//                current.next = current.next.next;
//            } else {
//                // Move to the next node
//                current = current.next;
//            }
//        }
//
//        return head; // Return the modified list
//    }

}
